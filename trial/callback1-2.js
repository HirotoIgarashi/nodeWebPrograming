'use strict';

// 関数の定義
const processA = (text) => {
  console.log(`${text} start`);
};

const processB = (text) => {
  console.log(`${text} start`);
};

const processC = (text) => {
  console.log(`${text} start`);
};

// 関数の実行
processA('process A');
processB('process B');
processC('process C');
